# `title`

```mcfc
entity.title(message: string|text_def) -> void
```

Sends a title to the receiver.

```mcfc
fn announce(player: player_ref) -> void:
    player.title("Quest complete")
```

Alias: [`send_title`](./send-title).

