# `player_ref`

Reference to a single player. Player refs support entity methods plus player surfaces such as inventory and hotbar.

```mcfc
fn reward(player: player_ref) -> void:
    player.give("minecraft:emerald", 1)
```

