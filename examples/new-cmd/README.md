# new-cmd

This project uses plain MCFC with no helper runtime.

## Build

```powershell
mcfc build . --clean
```

## Run

1. Copy `dist/` into your world's `datapacks/` folder.
2. Run `/reload` in Minecraft.
